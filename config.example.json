{
  "root_dir": "C:/Agent",
  "env_file": ".env",
  "workspace_dir": "workspace",
  "notes": [
    "Copy .env.example to .env and fill in local secrets.",
    "YUEYUE_ROOT_DIR is optional; by default YueYue uses the folder containing the Python files.",
    "Do not commit .env, chat history, project_cache, logs, Telegram chat id, or private stickers."
  ]
}
